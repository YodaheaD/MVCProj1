@{
    ViewData["Title"] = "Welcome";
}

<h2>A loop in the Welcome.cshtml view template that displays "Hello" NumTimes.</h2>

<ul>
    @for (int i = 0; i < (int)ViewData["NumTimes"]!; i++)
    {
        <li>@ViewData["Message"]</li>
    }
</ul>